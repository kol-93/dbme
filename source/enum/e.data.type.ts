export enum DATA_TYPE {
    BOOLEAN                             = 'boolean',
    INTEGER                             = 'integer',
    SERIAL                              = 'serial',
    UUID                                = 'uuid',
    VARCHAR                             = 'character varying',
}
